import {
    codeBlockNodeType,
    customNodeSpecs,
    taskRowNodeType,
} from '@lixpi/prosemirror'

export const nodeTypes = {
    taskRowNodeType,
    codeBlockNodeType,
}

export default customNodeSpecs
