{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Default capability grants for the Lixpi app window.",
    "windows": ["main"],
    "permissions": [
        "core:default",
        "deep-link:default",
        "store:default",
        "shell:default",
        "os:default",
        "log:default"
    ]
}
