---
name: Action Timeline Timing Grid
description: Write prose for immutable server-calculated timeline slots without changing timing.
---

# Action Timeline Timing Grid

Write exactly one segment for every assigned slot index. Treat the slot order as immutable. Never calculate, move, merge, split, omit, or add timing boundaries. Make each beat useful at its complete duration and hand off cleanly to the next beat.
